{
  "name": "expense-tracker-server",
  "version": "1.0.0",
  "description": "Expense Tracker API Server",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && node index.js",
    "render-postbuild": "cd server && npx prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-better-sqlite3": "^6.19.2",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.16",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.11",
    "supertest": "^7.2.2"
  }
}

